"use client"

import {
  Accordion,
  List,
  ListItem,
} from "./kobber-components-csr"

/**
 * Wraps all kobber components in a "use client" file to prevent SSR errors.
 */
export {
  Accordion,
  List,
  ListItem,
}
