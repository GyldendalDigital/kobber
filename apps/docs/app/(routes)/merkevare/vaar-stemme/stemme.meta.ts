import { pageMetadata } from "@/lib/metadata.utils"
import { placeholderImageUrl } from "@/lib/utils"

export const metaBrandVaarStemme = pageMetadata(import.meta.url, {
  title: "Vår stemme",
  image: placeholderImageUrl({}),
  status: "kommer",
  description: "",
})
