import { Controls, Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as CheckboxStories from "./Checkbox.stories";
import "../theme-context-provider/ThemeContext";
import "../text/text-link/TextLink";
import "@gyldendal/kobber-icons/web-components";
import { init as initIcons } from "@gyldendal/kobber-icons/init";

{initIcons()}

<style>
  {`
    kobber-keyboard {
      display: inline;
    }
  `}
</style>

<Meta of={CheckboxStories} />
<kobber-theme-context>

# Checkbox Group and Inputs
When used separately, Checkbox Inputs should specify name. When used in a Checkbox Group, their common name is specified on the group.

<kobber-keyboard></kobber-keyboard> Keyboard navigation: Use tab to reach the Checkbox group, and <kobber-text-link href="https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#:~:text=A%20primary%20keyboard,multiple%20focusable%20elements.">arrow keys</kobber-text-link> to move focus inside the group.

<Canvas  of={CheckboxStories.Component} />
<Controls of={CheckboxStories.Component}/>

</kobber-theme-context>
