import { Controls, Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as RadioStories from "./Radio.stories";
import "../theme-context-provider/ThemeContext";
import "../text/text-link/TextLink";
import "@gyldendal/kobber-icons/web-components";
import { init as initIcons } from "@gyldendal/kobber-icons/init";

{initIcons()}

<style>
  {`
    kobber-keyboard {
      display: inline;
    }
  `}
</style>

<Meta of={RadioStories} />
<kobber-theme-context>

# Radio Group and Inputs

A Radio Group contains more than one Radio Input, and the Radio Inputs are mutually exclusive. Radio Input must be used inside a Radio Group.

<kobber-keyboard></kobber-keyboard> Keyboard navigation: Use tab to reach the radio group, and <kobber-text-link href="https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#:~:text=A%20primary%20keyboard,multiple%20focusable%20elements.">arrow keys</kobber-text-link> to move focus inside the group.

<Canvas of={RadioStories.Component} />
<Controls of={RadioStories.Component}/>

</kobber-theme-context>
