import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-message_warning",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const message_warning: Story = {};
