import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-volume_on",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const volume_on: Story = {};
