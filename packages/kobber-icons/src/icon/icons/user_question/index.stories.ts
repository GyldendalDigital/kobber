import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-user_question",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const user_question: Story = {};
