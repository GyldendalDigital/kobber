import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-task_list",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const task_list: Story = {};
