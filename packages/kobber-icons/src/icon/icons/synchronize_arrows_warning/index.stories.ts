import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-synchronize_arrows_warning",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const synchronize_arrows_warning: Story = {};
