import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-synchronize_arrow_clock",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const synchronize_arrow_clock: Story = {};
