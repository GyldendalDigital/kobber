import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-layout_left",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const layout_left: Story = {};
