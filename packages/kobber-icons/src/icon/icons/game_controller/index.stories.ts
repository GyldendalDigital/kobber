import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-game_controller",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const game_controller: Story = {};
