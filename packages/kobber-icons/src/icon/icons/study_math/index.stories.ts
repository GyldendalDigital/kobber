import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-study_math",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const study_math: Story = {};
