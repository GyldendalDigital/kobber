import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-question_circle",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const question_circle: Story = {};
