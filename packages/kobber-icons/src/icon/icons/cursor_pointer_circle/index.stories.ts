import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-cursor_pointer_circle",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const cursor_pointer_circle: Story = {};
