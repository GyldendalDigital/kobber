import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-view_off",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const view_off: Story = {};
