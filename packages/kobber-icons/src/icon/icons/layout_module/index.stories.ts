import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-layout_module",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const layout_module: Story = {};
