import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-draw_tablet",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const draw_tablet: Story = {};
