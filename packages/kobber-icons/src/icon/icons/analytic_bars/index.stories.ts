import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-analytic_bars",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const analytic_bars: Story = {};
