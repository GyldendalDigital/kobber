import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-expand_resize",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const expand_resize: Story = {};
