import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-tablet_touch",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const tablet_touch: Story = {};
