import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-subtract",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const subtract: Story = {};
