import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-clock_hand",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const clock_hand: Story = {};
