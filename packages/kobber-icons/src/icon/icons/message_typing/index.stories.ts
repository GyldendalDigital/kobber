import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-message_typing",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const message_typing: Story = {};
