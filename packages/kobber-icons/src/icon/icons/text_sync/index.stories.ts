import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-text_sync",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const text_sync: Story = {};
