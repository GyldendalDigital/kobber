import type { Meta, StoryObj } from "@storybook/web-components";
import ".";

const meta: Meta = {
	title: "Icon/Icons",
	component: "kobber-add_figure",
};

export default meta;
type Story = StoryObj<typeof meta>;

export const add_figure: Story = {};
