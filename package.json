{
  "name": "kobber",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "blocks/*"
  ],
  "scripts": {
    "clean": "turbo clean && rm -rf .turbo node_modules",
    "lint": "turbo lint",
    "dev": "turbo dev",
    "build": "turbo build",
    "tokens": "turbo tokens",
    "storybook": "turbo storybook",
    "css": "turbo css",
    "test": "turbo block-test"
  },
  "dependencies": {
    "@changesets/cli": "latest",
    "prettier": "latest",
    "turbo": "latest"
  },
  "devDependencies": {
    "axios": "^1.6.2"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "node": ">=18.0.0"
  }
}
